# 🔧 검색 오류 해결 가이드

## "뉴스를 찾을 수 없습니다" 오류가 발생하는 경우

### 1단계: 터미널 확인

개발 서버가 실행 중인 터미널 창을 확인하세요. 오류 메시지가 표시되는지 확인합니다.

**확인할 오류 메시지:**
- `Gemini API 키가 설정되지 않았습니다`
- `API_KEY_INVALID` 또는 `PERMISSION_DENIED`
- `JSON 파싱 오류`

### 2단계: 환경 변수 확인

터미널에서 다음 명령어로 API 키가 올바르게 설정되었는지 확인:

```bash
cd ~/Desktop/챗봇
cat .env.local
```

**올바른 형식:**
```
NEXT_PUBLIC_GEMINI_API_KEY=AIzaSyDgO2AxMYt-Fd7aMBA9Lu4o5ku0bGulNu4
```

**문제가 있다면:**
1. `.env.local` 파일이 올바른 위치에 있는지 확인
2. API 키가 정확한지 확인
3. 개발 서버를 재시작 (`Ctrl + C` 후 `npm run dev`)

### 3단계: API 키 유효성 확인

Gemini API 키가 유효한지 확인하세요:

1. https://makersuite.google.com/app/apikey 접속
2. API 키가 활성화되어 있는지 확인
3. 필요하면 새 API 키 생성

### 4단계: 개발 서버 재시작

환경 변수를 변경했다면 서버를 재시작해야 합니다:

1. 터미널에서 `Control + C`로 서버 중지
2. 다시 실행: `npm run dev`
3. 브라우저에서 새로고침

### 5단계: 브라우저 개발자 도구 확인

1. 브라우저에서 `F12` 또는 `Cmd + Option + I` 눌러서 개발자 도구 열기
2. **Console** 탭에서 오류 메시지 확인
3. **Network** 탭에서 `/api/search-news` 요청 확인
   - 요청이 실패했는지 확인
   - 응답 내용 확인

### 6단계: 다른 키워드로 시도

일부 키워드는 결과가 없을 수 있습니다. 다른 키워드로 시도해보세요:

- "인공지능"
- "기술"
- "경제"
- "스포츠"

---

## 일반적인 오류 및 해결 방법

### 오류 1: "API 키가 설정되지 않았습니다"

**원인:** `.env.local` 파일이 없거나 잘못된 위치에 있음

**해결:**
```bash
cd ~/Desktop/챗봇
echo 'NEXT_PUBLIC_GEMINI_API_KEY=AIzaSyDgO2AxMYt-Fd7aMBA9Lu4o5ku0bGulNu4' > .env.local
```

그리고 서버 재시작

### 오류 2: "PERMISSION_DENIED" 또는 "API_KEY_INVALID"

**원인:** API 키가 유효하지 않거나 권한이 없음

**해결:**
1. https://makersuite.google.com/app/apikey 에서 API 키 확인
2. API 키가 활성화되어 있는지 확인
3. 필요하면 새 API 키 생성 후 `.env.local` 업데이트

### 오류 3: "JSON 파싱 오류"

**원인:** Gemini API 응답 형식 문제

**해결:**
- 이미 코드에서 자동으로 처리하도록 개선했습니다
- 여전히 발생하면 다른 키워드로 시도
- 서버 재시작

### 오류 4: 네트워크 오류

**원인:** 인터넷 연결 문제 또는 API 서버 문제

**해결:**
1. 인터넷 연결 확인
2. 잠시 후 다시 시도
3. 방화벽이나 VPN이 API 접근을 차단하는지 확인

---

## 디버깅 팁

### 터미널 로그 확인

개발 서버 터미널에서 다음 정보를 확인하세요:

```
검색된 뉴스 개수: X
Gemini 응답 원본: ...
JSON 파싱 오류: ...
```

이 정보가 있으면 문제를 더 정확히 진단할 수 있습니다.

### 테스트 방법

터미널에서 직접 API 테스트:

```bash
curl -X POST http://localhost:3000/api/search-news \
  -H "Content-Type: application/json" \
  -d '{"keyword":"인공지능"}'
```

응답을 확인하여 문제를 진단할 수 있습니다.

---

## 여전히 안 되나요?

다음 정보를 알려주세요:

1. 터미널에 표시된 오류 메시지 전체
2. 브라우저 개발자 도구 Console 탭의 오류 메시지
3. 브라우저 개발자 도구 Network 탭의 `/api/search-news` 응답 내용
4. `.env.local` 파일 내용 (API 키는 마스킹해서)
