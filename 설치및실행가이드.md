# 챗봇 설치 및 실행 가이드

## 📋 단계별 설치 절차

### 1단계: Node.js 설치 확인 및 설치

터미널을 열고 다음 명령어를 실행하세요:

```bash
node --version
```

**결과가 나오면** (예: v18.x.x 또는 v20.x.x) → **2단계로 이동**

**"command not found" 오류가 나오면** → Node.js 설치 필요

#### Node.js 설치 방법 (macOS)

**방법 1: 공식 웹사이트에서 설치 (추천)**
1. 브라우저에서 https://nodejs.org/ 접속
2. "LTS" 버전 다운로드 버튼 클릭
3. 다운로드된 .pkg 파일 실행
4. 설치 마법사를 따라 설치 완료
5. 터미널을 다시 열고 `node --version` 확인

**방법 2: Homebrew로 설치**
```bash
brew install node
```

---

### 2단계: 프로젝트 폴더로 이동

터미널에서 다음 명령어를 실행하세요:

```bash
cd /Users/parkkun/Desktop/챗봇
```

---

### 3단계: 의존성 패키지 설치

프로젝트 폴더에서 다음 명령어를 실행하세요:

```bash
npm install
```

이 과정은 몇 분 정도 걸릴 수 있습니다. 완료되면 "added XXX packages" 메시지가 표시됩니다.

**오류가 발생하면:**
- 인터넷 연결 확인
- `npm cache clean --force` 실행 후 다시 시도

---

### 4단계: 환경 변수 확인

`.env.local` 파일이 있는지 확인하세요:

```bash
ls -la .env.local
```

파일이 있고 내용이 다음과 같으면 OK:
```
NEXT_PUBLIC_GEMINI_API_KEY=AIzaSyDgO2AxMYt-Fd7aMBA9Lu4o5ku0bGulNu4
```

파일이 없거나 내용이 다르면:
```bash
cat > .env.local << 'EOF'
NEXT_PUBLIC_GEMINI_API_KEY=AIzaSyDgO2AxMYt-Fd7aMBA9Lu4o5ku0bGulNu4
EOF
```

---

### 5단계: 개발 서버 실행

다음 명령어를 실행하세요:

```bash
npm run dev
```

**성공하면 다음과 같은 메시지가 표시됩니다:**
```
  ▲ Next.js 14.x.x
  - Local:        http://localhost:3000
  - Ready in X.Xs

  ○ Compiling / ...
  ✓ Compiled / in X.Xs
```

**중요:** 이 터미널 창은 **열어둔 채로** 유지해야 합니다. 서버를 중지하려면 `Ctrl + C`를 누르세요.

---

### 6단계: 브라우저에서 확인

1. 브라우저를 엽니다 (Chrome, Safari, Firefox 등)
2. 주소창에 다음을 입력하거나 클릭합니다:
   ```
   http://localhost:3000
   ```
3. "📰 뉴스 챗봇" 페이지가 표시되면 성공입니다!

---

## 🔧 문제 해결

### 문제 1: "포트 3000이 이미 사용 중입니다"
**해결 방법:**
```bash
# 다른 포트 사용
PORT=3001 npm run dev
```
그리고 브라우저에서 `http://localhost:3001` 접속

### 문제 2: "모듈을 찾을 수 없습니다"
**해결 방법:**
```bash
rm -rf node_modules package-lock.json
npm install
```

### 문제 3: "API 키 오류"
**해결 방법:**
`.env.local` 파일을 확인하고 API 키가 올바른지 확인하세요.

### 문제 4: 빈 화면이 보입니다
**해결 방법:**
1. 브라우저 개발자 도구 열기 (F12 또는 Cmd+Option+I)
2. Console 탭에서 오류 메시지 확인
3. Network 탭에서 API 요청이 실패하는지 확인

---

## ✅ 정상 작동 확인 체크리스트

- [ ] Node.js가 설치되어 있음 (`node --version` 실행 시 버전 표시)
- [ ] `npm install` 완료 (오류 없음)
- [ ] `.env.local` 파일이 존재하고 API 키가 설정됨
- [ ] `npm run dev` 실행 시 "Ready" 메시지 표시
- [ ] 브라우저에서 `http://localhost:3000` 접속 시 페이지가 표시됨
- [ ] 키워드 검색 시 뉴스가 표시됨
- [ ] 챗봇에 질문 시 답변이 생성됨

---

## 📞 추가 도움이 필요한 경우

각 단계에서 오류 메시지를 복사해서 알려주시면 더 정확한 해결책을 제시할 수 있습니다.
